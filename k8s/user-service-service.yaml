# Service to expose user-service inside/outside cluster.
apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: devops-demo
  labels:
    app: user-service
spec:
  type: ClusterIP
  selector:
    app: user-service
  ports:
    - name: http
      port: 80
      targetPort: 5000
